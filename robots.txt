# This site is intentionally kept out of search engines.
# Nothing is indexed, so crawling is blocked outright here; every page also
# carries <meta name="robots" content="noindex, nofollow"> as a backstop.
User-agent: *
Disallow: /
